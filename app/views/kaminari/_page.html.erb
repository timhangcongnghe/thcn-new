<% if page.current? %>
    <li>
        <span aria-current="page" class="page-numbers current"><%= page %></span>
    <li>
<% else %>
    <li>
        <%= link_to page, url, remote: remote, rel: page.rel, class: 'page-numbers' %>
    </li>
<% end %>